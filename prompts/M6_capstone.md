# PROMPT — M6: Optional capstone

Paste into Claude Code after M5. Pick ONE to start; each is self-contained.

---

Read CLAUDE.md and the relevant docs. Options:

A) Telegram digest channel — mirror the M5 digest to Telegram via a bot/MCP server.
   Per-user opt-in + chat-id capture in preferences.channels.

B) Tailored-resume document export — turn approved `tailorings` into a downloadable
   docx/PDF using an ATS-safe template. Still no fabrication; export only approved,
   user-reviewed content.

C) Share-link hardening + onboarding polish — invite flow, empty states, error
   handling, basic analytics, abuse/cost protection for a public share link.

Do only the chosen option. Acceptance: the feature works end to end for two test
accounts with isolation intact. Update STATE.md + CHANGELOG.md, commit, push.
